# ── Agentic SOP Compiler — Environment Variables ─────────────────────────────
# Copy this file to .env and fill in your keys:
#   cp .env.example .env

# Required — Cerebras API (LLM for code generation + reporting)
CEREBRAS_API_KEY=your-cerebras-api-key-here

# Optional — LangSmith tracing (observability)
LANGCHAIN_API_KEY=your-langsmith-api-key-here
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=agentic-sop-compiler

# Optional — E2B sandbox (cloud code execution)
E2B_API_KEY=your-e2b-api-key-here

# Optional — Gemini (embeddings, if using vector store features)
GEMINI_API_KEY=your-gemini-api-key-here
